server:
  port: 8082
spring:
  application:
    name: dataplatform
  cloud:
    config:
      #profile: dev
      uri: http://127.0.0.1:8081/
  profiles:
    active: business-dev,common-dev

    #include: dataplatform-commons